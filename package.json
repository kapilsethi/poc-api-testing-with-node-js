{
  "name": "poc-api-testing-with-node-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "echo 'lint checks...' && ./node_modules/.bin/eslint --ignore-path .gitignore .",
    "test": "./node_modules/.bin/mocha --reporter mocha-allure-reporter",
    "generatereport": "allure generate --clean && allure open",
    "rtag": "./node_modules/.bin/mocha --reporter mocha-allure-reporter && allure generate --clean && allure open"
  },
  "pre-commit": [
    "lint"
  ],
  "keywords": [],
  "author": "Kapil Sethi",
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.13.0",
    "chai": "^4.2.0",
    "dotenv": "^8.2.0",
    "eslint": "^7.1.0",
    "mocha": "^7.2.0",
    "mocha-allure-reporter": "^1.4.0",
    "pre-commit": "^1.2.2",
    "request": "^2.88.2",
    "request-promise": "^4.2.5"
  }
}
